#!/bin/bash

function getScriptDirectory {
    local src=${BASH_SOURCE[0]}
    local dir=$(dirname "$src")
    local prev=$PWD
    cd "$dir"
    echo $PWD
    cd "$prev"
}

WORKING=$PWD
SCRIPT_PATH=`getScriptDirectory`
OPM_PATH=$SCRIPT_PATH/opm.py

python $OPM_PATH $@

