#!/usr/bin/env bash

if [[ $(id -u) -ne 0 ]]; then
    echo "Please run as root" 1>&2
    exit 1
fi

if ! command -v usermod &> /dev/null; then
    echo "Not found usermod command" 1>&2
    exit 1
fi

usermod -aG docker "$USER"
newgrp docker
