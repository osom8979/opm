#!/usr/bin/env bash

# [WARNING]
# This script should not refer to other OPM scripts.

if [[ -z $OPM_HOME ]]; then
OPM_HOME=$(cd "$(dirname "${BASH_SOURCE[0]}")/.." || exit; pwd)
fi

grep --color=never '^VERSION=' "$OPM_HOME/INFORMATION" | sed 's/^VERSION=//g'
