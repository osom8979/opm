#!/usr/bin/env bash

if ! command -v opm-version &> /dev/null; then
PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" || exit; pwd):$PATH"
fi

if [[ -z "$TMUX" ]]; then
    opm-println-error "Not inside a tmux session"
    exit 1
fi

tmux send-keys -l "cd '${OPM_HOME}'" && tmux send-keys Enter
