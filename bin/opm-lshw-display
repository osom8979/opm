#!/usr/bin/env bash

if ! command -v opm-version &> /dev/null; then
PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" || exit; pwd):$PATH"
fi

if ! command -v lshw &> /dev/null; then
    opm-println-error "Not found lshw command"
    exit 1
fi

lshw -C display
