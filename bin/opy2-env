#!/usr/bin/env bash

if [[ -z $(which opm-version 2> /dev/null) ]]; then
PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")"; pwd):$PATH"
fi

OPY_VERSION=2.7.17 OPY_VERBOSE=${OPY_VERBOSE:-0} source opm-init-opy

