#!/usr/bin/env bash

if [[ -z $(which opm-version 2> /dev/null) ]]; then
PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")"; pwd):$PATH"
fi

OPN_VERSION=14.13.1 OPN_VERBOSE=${OPN_VERBOSE:-0} source opn-env

