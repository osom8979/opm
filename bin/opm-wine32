#!/usr/bin/env bash

if ! command -v opm-version &> /dev/null; then
PATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" || exit; pwd):$PATH"
fi

if ! command -v wine &> /dev/null; then
    opm-println-error "Not found wine executable"
    exit 1
fi

source opm-wine32-env && wine "$@"
